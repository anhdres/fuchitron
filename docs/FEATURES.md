
## Remote spectator link (2026-04-01)
- Crear evento → generar link único → compartir → otros siguen el score en tiempo real
- Tipo "broadcast mode" — el partido se transmite a un canal/página pública
- Usuarios sin la app pueden ver el score via URL
- Para cuando arranquemos con features server-side o polling
